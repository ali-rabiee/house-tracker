# همگام‌سازی بین گوشی‌ها با Google Sheets

با این کار، علی و فاطیما و محمد هر کدام از گوشی خودشان ثبت می‌کنند و همه روی **یک لیست مشترک** می‌نشیند.
اطلاعات در یک Google Sheet مالِ خودت ذخیره می‌شود — نه سرور ما، نه سرویس واسط.

راه‌اندازی یک‌بار است و حدود ۵ دقیقه طول می‌کشد. فقط یک نفر (تو) این کارها را می‌کند.

---

## ۱) یک Google Sheet بساز

به [sheets.new](https://sheets.new) برو و اسمش را بگذار «مراقبت از خانه».
لازم نیست چیزی در آن بنویسی؛ برگه‌های `logs` و `config` خودکار ساخته می‌شوند.

## ۲) اسکریپت را بچسبان

در همان شیت: منوی **Extensions → Apps Script**.
هرچه در فایل `Code.gs` هست پاک کن و کل محتوای [`google-apps-script/Code.gs`](google-apps-script/Code.gs) این ریپو را جای آن بچسبان.

بعد در خط اول اسکریپت، رمز را عوض کن:

```js
var TOKEN = 'khaneh-1404';     // ← این را به یک عبارت دلخواه خودت تغییر بده
```

با `Ctrl+S` ذخیره کن.

## ۳) یک بار اجرا کن تا اجازه بگیرد

از نوار بالای Apps Script، تابع **`setup`** را انتخاب کن و **Run** بزن.
گوگل اجازه می‌خواهد:

1. **Review permissions** → حساب خودت را انتخاب کن
2. صفحهٔ «Google hasn't verified this app» → **Advanced** → **Go to «مراقبت از خانه» (unsafe)**
3. **Allow**

> این هشدار طبیعی است: اسکریپت را خودت نوشته‌ای و منتشر نشده، برای همین گوگل «تأییدنشده» می‌داند.

## ۴) منتشرش کن (Deploy)

**Deploy → New deployment** → کنار Select type روی چرخ‌دنده بزن → **Web app**، و:

| گزینه | مقدار |
|---|---|
| Description | هرچه دوست داری |
| Execute as | **Me** (حساب خودت) |
| Who has access | **Anyone** |

**Deploy** را بزن و **Web app URL** را کپی کن — چیزی شبیه:

```
https://script.google.com/macros/s/AKfy…long…/exec
```

> «Anyone» یعنی هر کسی که این آدرس **و** رمز را داشته باشد می‌تواند بخواند و بنویسد.
> بدون این گزینه، گوشی بقیه نمی‌تواند وصل شود. پس آدرس را جای عمومی نگذار.
> امنیتش در حد «قفل درِ خانه» است، نه گاوصندوق.

## ۵) اپ را وصل کن

اپ را باز کن → تبِ **تنظیمات** → کارت **☁️ همگام‌سازی بین گوشی‌ها**:

- **آدرس Web App**: همان لینکِ `/exec`
- **رمز مشترک**: همان چیزی که در `TOKEN` گذاشتی
- **اتصال و همگام‌سازی** را بزن

بالای صفحه باید نشان **☁️ همگام** ببینی. حالا برو سراغ شیت — سطرها آنجا هستند.

## ۶) گوشی بقیه را وصل کن

در همان کارت، **🔗 کپی لینک آماده برای بقیه** را بزن و لینک را برای فاطیما و محمد بفرست.
با باز کردن آن لینک، گوشی‌شان خودکار به همین شیت وصل می‌شود و لازم نیست چیزی تایپ کنند.

بگو لینک را به صفحهٔ خانهٔ گوشی اضافه کنند (Share → Add to Home Screen) تا مثل یک اپ باز شود.

---

## چطور کار می‌کند

- هر ثبت روی گوشی، **اول محلی** ذخیره می‌شود؛ بعد در پس‌زمینه به شیت می‌رود.
- هر ۲۵ ثانیه و هر بار که اپ را باز می‌کنی، تغییرات بقیه گرفته می‌شود.
- **آفلاین هم کار می‌کند**: بدون اینترنت ثبت کن، نشان بالای صفحه می‌شود ⚠️ آفلاین، و به‌محض وصل شدن خودش می‌فرستد.
- **حذف هم منتقل می‌شود**: سطر حذف‌شده در شیت می‌ماند ولی علامت `deleted=1` می‌خورد، برای همین دوباره برنمی‌گردد.
- هر سطر یک `rev` از سمت سرور می‌گیرد؛ ساعت گوشی‌ها ملاک نیست، پس گوشیِ با ساعت اشتباه چیزی را خراب نمی‌کند.
- اگر دو نفر هم‌زمان یک ثبت را ویرایش کنند، آخرین ویرایشی که به شیت می‌رسد می‌ماند.
- لیست افراد و کارها هم مشترک است: هر کس ویرایش کند، بقیه می‌گیرند.

## نکته‌های مهم

- **روی لینک claude.ai کار نمی‌کند.** آن صفحه اجازهٔ اتصال به سرویس بیرونی را ندارد و خودش این را به تو می‌گوید.
  برای همگام‌سازی، اپ را از **GitHub Pages** باز کن: `Settings → Pages` → شاخهٔ `claude/home-tasks-app-srn8kq` و پوشهٔ `/ (root)`.
- **بعد از هر تغییر در اسکریپت** باید دوباره منتشر کنی: **Deploy → Manage deployments → ✏️ → Version: New version → Deploy**.
  وگرنه تغییر اعمال نمی‌شود.
- ستون‌های `type` تا `deleted` در شیت برای برنامه است؛ دستی عوضشان نکن. ستون‌های فارسیِ اول فقط برای خواندن توست.
- پشتیبان‌گیری همچنان کار می‌کند و حالا شیت خودش یک پشتیبان زنده است.

## اگر وصل نشد

| نشانه | علت معمول |
|---|---|
| «رمز اشتباه است» | `TOKEN` در اسکریپت با رمز داخل اپ یکی نیست |
| خطای شبکه / ⚠️ آفلاین | آدرس به `/exec` ختم نمی‌شود، یا Who has access روی Anyone نیست |
| تغییرات اسکریپت اثر ندارد | نسخهٔ جدید Deploy نشده (بخش «نکته‌های مهم») |
| صفحه از claude.ai باز شده | از لینک GitHub Pages استفاده کن |
