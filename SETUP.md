# راهنمای کامل راه‌اندازی

از صفر تا جایی که هر سه نفر از گوشی خودشان روی یک لیست مشترک ثبت کنند.
سه بخش دارد و **فقط یک نفر** (مدیر خانه) بخش‌های ۱ و ۲ را انجام می‌دهد؛ بقیه فقط بخش ۳ را.

| بخش | چه‌کار می‌کنی | چقدر طول می‌کشد |
|---|---|---|
| ۱ | سایت را روی GitHub منتشر می‌کنی تا یک لینک دائمی داشته باشی | ۳ دقیقه |
| ۲ | Google Sheet و اسکریپتش را می‌سازی و اپ را وصل می‌کنی | ۷ دقیقه |
| ۳ | لینک را به بقیه می‌دهی و روی گوشی‌شان نصب می‌شود | ۱ دقیقه برای هر نفر |

---

# بخش ۱ — انتشار سایت روی GitHub Pages

اپ یک فایل ساده است و به سرور نیاز ندارد؛ GitHub خودش رایگان میزبانی‌اش می‌کند.

### ۱.۱ رفتن به تنظیمات ریپو

1. در مرورگر برو به `https://github.com/ali-rabiee/house-tracker`
2. از نوار بالا **Settings** را بزن (چرخ‌دنده، سمت راست نوار)
3. از ستون کناری **Pages** را انتخاب کن

### ۱.۲ انتخاب شاخه

در بخش **Build and deployment**:

| فیلد | مقدار |
|---|---|
| Source | **Deploy from a branch** |
| Branch | `main` |
| Folder | `/ (root)` |

**Save** را بزن.

> کد روی `main` است، پس همین شاخه را انتخاب کن. هر تغییر بعدی که روی `main` برود، سایت خودکار به‌روز می‌شود.

### ۱.۳ گرفتن لینک

یکی دو دقیقه صبر کن و صفحه را رفرش کن. بالای همان صفحه یک کادر سبز با آدرس ظاهر می‌شود:

```
https://ali-rabiee.github.io/house-tracker/
```

این **لینک اصلی** توست. بازش کن؛ باید اپ را ببینی.

> اگر ۴۰۴ داد: معمولاً یعنی هنوز build تمام نشده. در تبِ **Actions** ریپو ببین
> که آخرین اجرای «pages build and deployment» سبز شده باشد، بعد دوباره امتحان کن.

### ۱.۴ نصب روی گوشی

لینک را روی گوشی باز کن و به صفحهٔ خانه اضافه‌اش کن تا مثل یک اپ باز شود:

- **آیفون (Safari)**: دکمهٔ Share (مربع با فلش) ← **Add to Home Screen**
- **اندروید (Chrome)**: منوی سه‌نقطه ← **Add to Home screen** / **Install app**

> حتماً از Safari یا Chrome خودِ گوشی باز کن، نه از مرورگر داخل تلگرام یا اینستاگرام —
> آن‌ها حافظهٔ موقت دارند و اطلاعات محلی‌ات پاک می‌شود.

**تا اینجا اپ کار می‌کند، ولی هنوز اطلاعات هر گوشی جداست.** برای مشترک شدن، بخش ۲.

---

# بخش ۲ — ساخت Google Sheet و وصل کردن

اطلاعات در یک Google Sheet **مالِ حساب خودت** ذخیره می‌شود. سرور واسطی وجود ندارد.

### ۲.۱ ساخت شیت

1. با حساب گوگل خودت وارد شو
2. برو به [sheets.new](https://sheets.new) — یک شیت خالی ساخته می‌شود
3. اسمش را بگذار **مراقبت از خانه**

لازم نیست چیزی در آن بنویسی؛ همهٔ برگه‌ها خودکار ساخته می‌شوند.

### ۲.۲ چسباندن اسکریپت

1. در همان شیت: منوی **Extensions ← Apps Script**
2. تبِ جدیدی باز می‌شود با یک فایل `Code.gs` که داخلش `function myFunction() {}` نوشته
3. **همه‌اش را پاک کن** (`Ctrl+A` بعد `Delete`)
4. محتوای فایل [`google-apps-script/Code.gs`](google-apps-script/Code.gs) این ریپو را کامل کپی کن و اینجا بچسبان
5. در خط‌های اول، رمز را عوض کن:

```js
var TOKEN = 'khaneh-1404';     // ← این را به یک عبارت دلخواه خودت تغییر بده
```

6. `Ctrl+S` بزن تا ذخیره شود

> رمز را یادداشت کن؛ هم خودت و هم بقیه به آن نیاز دارید (البته لینک اشتراکی خودش رمز را می‌برد).

### ۲.۳ اجرای اولیه و دادن اجازه

1. بالای صفحهٔ Apps Script یک منوی کشویی هست که اسم یک تابع در آن نوشته شده.
   **حتماً مطمئن شو روی `setup` باشد** — اگر تابع دیگری انتخاب باشد،
   «Execution completed» می‌بینی ولی هیچ برگه‌ای ساخته نمی‌شود.
2. **Run** را بزن
3. پنجرهٔ اجازه باز می‌شود:
   - **Review permissions** → حساب خودت را انتخاب کن
   - صفحهٔ «Google hasn't verified this app» → **Advanced** → **Go to «مراقبت از خانه» (unsafe)**
   - **Allow**
4. بعد از Allow، معمولاً باید **یک بار دیگر Run را بزنی** تا این‌بار واقعاً اجرا شود.

در پایین صفحه (Execution log) باید خطی شبیه این ببینی:

```
برگه‌ها آماده شد: logs، config، نوبت‌ها، خلاصهٔ افراد، روزانه، آمار کارها
```

حالا برگرد به شیت و **صفحه را یک بار refresh کن** (`F5`). باید برگه‌های بالا را پایین پنجره ببینی.

> **اگر برگه‌ها را نمی‌بینی:** تقریباً همیشه یکی از این دوتاست —
> یا تابع انتخاب‌شده `setup` نبوده، یا شیت refresh نشده.
> راه دوم و مطمئن‌تر: بعد از refresh، در نوار منوی خود شیت یک منوی تازه به اسم
> **«مراقبت از خانه»** ظاهر می‌شود؛ از آنجا **«ساخت / بازسازی برگه‌ها»** را بزن.

### ۲.۴ انتشار به‌عنوان Web App

1. در Apps Script: **Deploy ← New deployment**
2. کنار «Select type» روی **⚙️ (چرخ‌دنده)** بزن و **Web app** را انتخاب کن
3. مقادیر:

| فیلد | مقدار |
|---|---|
| Description | هرچه دوست داری (مثلاً `house v1`) |
| Execute as | **Me** — حساب خودت |
| Who has access | **Anyone** |

4. **Deploy** را بزن
5. آدرس **Web app URL** را کپی کن — چیزی شبیه:

```
https://script.google.com/macros/s/AKfycb…خیلی‌طولانی…/exec
```

> **دربارهٔ گزینهٔ Anyone:** یعنی هر کسی که هم این آدرس طولانی و هم رمز را داشته باشد
> می‌تواند بخواند و بنویسد. بدون این گزینه، گوشی بقیه اصلاً نمی‌تواند وصل شود.
> امنیتش در حد «کلید خانه» است: لینک را جای عمومی (کانال، گروه عمومی، توییتر) نگذار.

### ۲.۵ وصل کردن اپ

1. اپ را از لینک GitHub Pages باز کن
2. تبِ **تنظیمات** ← کارت **☁️ همگام‌سازی بین گوشی‌ها**
3. پر کن:
   - **آدرس Web App**: همان لینکی که به `/exec` ختم می‌شود
   - **رمز مشترک**: همان چیزی که در `TOKEN` گذاشتی
4. **اتصال و همگام‌سازی** را بزن

بالای صفحه باید نشان **☁️ همگام** ببینی. حالا برگرد به شیت — سطرها آنجا هستند.

---

# بخش ۳ — دادن دسترسی به بقیه

### ۳.۱ لینک آماده (ساده‌ترین راه)

1. در همان کارت همگام‌سازی، **🔗 کپی لینک آماده برای بقیه** را بزن
2. لینک را برای فاطیما و محمد بفرست

آن لینک هم آدرس سایت و هم تنظیمات اتصال را با خودش دارد: با یک بار باز کردن،
گوشی‌شان خودکار به همان شیت وصل می‌شود و لازم نیست چیزی تایپ کنند.

بگو بعد از باز کردن، لینک را طبق بخش ۱.۴ به صفحهٔ خانه اضافه کنند.

### ۳.۲ راه دستی (اگر لینک کار نکرد)

لینک ساده `https://ali-rabiee.github.io/house-tracker/` را بفرست و بگو در
تنظیمات ← همگام‌سازی، آدرس `/exec` و رمز را دستی وارد کنند. نتیجه یکی است.

### ۳.۳ دسترسی به خود شیت (اختیاری)

اگر می‌خواهی بقیه جدول‌ها و آمار را هم ببینند، در Google Sheets دکمهٔ **Share** را بزن
و ایمیلشان را به‌صورت **Viewer** اضافه کن. برای ثبت کردن در اپ لازم نیست — اپ از طریق
اسکریپت می‌نویسد، نه با حساب شخصی آن‌ها.

---

# انتقال بین دستگاه‌ها

| حالت | راه‌حل |
|---|---|
| گوشی جدید، همگام‌سازی روشن | فقط لینک اشتراکی را باز کن؛ کل تاریخچه از شیت می‌آید |
| گوشی جدید، بدون همگام‌سازی | در گوشی قدیمی: تنظیمات ← **دانلود پشتیبان**؛ در گوشی جدید: **بازیابی از فایل** |
| می‌خواهی اطلاعات محلی را پاک کنی | تنظیمات ← **پاک کردن ثبت‌ها**؛ اول پشتیبان ذخیره می‌شود و متنش را هم نشان می‌دهد |
| بدون اینترنت | عادی ثبت کن؛ نشان بالای صفحه ⚠️ آفلاین می‌شود و بعد از وصل شدن خودش می‌فرستد |

> **دقت:** «پاک کردن ثبت‌ها» وقتی همگام‌سازی روشن است، حذف را به شیت و بقیهٔ گوشی‌ها هم منتقل می‌کند.
> اگر فقط می‌خواهی گوشی خودت سبک شود، اول **قطع اتصال** را بزن.

---

# استفادهٔ روزمره

1. **ورود**: اسمت را بزن ← **چک‌این**. ساعت و تاریخ ثبت می‌شود.
   نوبتت **تا وقتی چک‌اوت نکنی باز می‌ماند** — حتی چند روز. لازم نیست هر بار که اپ را باز می‌کنی دوباره چک‌این کنی.
2. **آب روی home**: بنر بالای تبِ کارها را بزن و تأیید کن (یک بار در هر روز).
   تا این کار را نکنی، ثبت کارهای مثبت آن روز فعال نمی‌شود.
3. **کارهای خانه**: هر کاری را که انجام دادی بزن ← امتیاز مثبت.
4. **چک‌اوت**: در پایین همان تب، همهٔ موارد از اول **منفی** هستند. هر کدام را انجام دادی بزن تا منفی‌اش برداشته شود.
   آخر کار **ثبت چک‌اوت** را بزن؛ هرچه باقی مانده باشد به همان تعداد امتیاز منفی ثبت می‌شود.
5. **امتیازها**: صفحهٔ هر نفر جداست — آمار، نمودار تفکیک کارها، نوبت‌های حضور و ریز فعالیت‌ها.

---

# چه چیزی در شیت ذخیره می‌شود

اپ خودش این برگه‌ها را می‌سازد و بعد از هر ثبت به‌روزشان می‌کند:

| برگه | محتوا |
|---|---|
| `logs` | تک‌تک ثبت‌ها با تاریخ، ساعت، روز هفته، نام، نوع، کد و عنوان کار، یادداشت، ثبت‌کننده و **نوبتی که به آن تعلق دارد** (تاریخ و ساعت چک‌این مربوطه) |
| `نوبت‌ها` | هر نوبت از چک‌این تا چک‌اوت: مدت، امتیاز مثبت و منفی، کارهای انجام‌شده، موارد جامانده، وضعیت |
| `خلاصهٔ افراد` | جمع‌بندی هر نفر: مثبت، منفی، تعداد چک‌این و چک‌اوت، نوبت‌های باز، درصد چک‌اوت کامل |
| `روزانه` | تفکیک روز به روز برای هر نفر — مناسب نمودار و Pivot |
| `آمار کارها` | جدول کار × شخص: هر کار را چه کسی چند بار انجام داده |
| `حذف‌شده‌ها` | سطل بازیافت: هر رکورد حذف‌شده با تاریخ و روزِ حذف و تاریخ و روزِ خودش، قابل بازیابی |
| `config` | لیست افراد و کارها (مشترک بین همهٔ گوشی‌ها) |

> برگه‌های گزارشی هر بار از نو نوشته می‌شوند؛ دستی در آن‌ها چیزی ننویس چون پاک می‌شود.
> اگر خواستی محاسبهٔ خودت را اضافه کنی، یک برگهٔ جدید بساز و از این‌ها با فرمول بخوان.

## حذف و بازیابی

- **حذف در اپ:** هر ثبت از تبِ تاریخچه یا از کارنامهٔ هر نفر قابل حذف است؛ «پاک کردن ثبت‌ها» و «ریست کامل» هم در تنظیمات هست.
- **هر حذفی اول می‌پرسد** چه چیزی قرار است حذف شود و آیا پشتیبان می‌خواهی: «📥 پشتیبان بگیر، بعد حذف کن» / «حذف بدون پشتیبان» / «انصراف».
  برای پاک کردن همه، گزینهٔ گرفتن **فایل اکسل** هم اضافه می‌شود.
- **در شیت هم واقعاً حذف می‌شود:** سطر از `logs` برداشته می‌شود و با تاریخ و روز کامل به برگهٔ **`حذف‌شده‌ها`** می‌رود.
  گزارش‌ها هم بلافاصله بدون آن بازسازی می‌شوند.
- **بازیابی:** در برگهٔ `حذف‌شده‌ها` جلوی سطر موردنظر، در ستون **«بازیابی؟»** بنویس `بله`،
  بعد از منوی «مراقبت از خانه» گزینهٔ **«بازیابی رکوردهای علامت‌خورده»** را بزن.
  رکورد به `logs` برمی‌گردد و از سطل بیرون می‌رود.

### همهٔ گوشی‌ها به‌روز می‌مانند

حذف و بازیابی هر دو یک شمارهٔ نسخهٔ تازه می‌گیرند، پس هر گوشی در همگام‌سازی بعدی‌اش آن را می‌بیند —
**حتی اگر وسط یک نوبت باشد یا اپش همان لحظه باز باشد و دستش به آن نخورد.**
اپِ باز خودش هر ۱۵ ثانیه چک می‌کند و اگر مدتی خبری نباشد به هر ۶۰ ثانیه کند می‌شود
(تا سهمیهٔ روزانهٔ Apps Script بیهوده مصرف نشود)؛ با هر تغییر، برگشتن به اپ یا عوض کردن تب، فوراً دوباره تند می‌شود.

## خوانایی جدول‌ها

بعد از هر ثبت، همهٔ برگه‌ها خودکار مرتب می‌شوند:

- **هیچ متنی از خانه‌اش بیرون نمی‌زند** — متن بلند داخل همان خانه می‌شکند (به‌جای سرریز روی خانهٔ بغلی).
- **عرض هر ستون به اندازهٔ محتوایش** تنظیم می‌شود، با یک سقف تا ستون‌های خیلی طولانی جدول را بی‌انتها نکنند.
- سطر عنوان **ثابت** می‌ماند، برگه **راست‌به‌چپ** است و سطرها **یک‌درمیان** رنگ می‌گیرند.
- در برگهٔ `logs` ستون‌های فنی (`type`, `taskId`, `ts`, `rev`, `sess` و…) **پنهان** می‌شوند تا فقط بخش خواندنی بماند.
  اپ همچنان از آن‌ها استفاده می‌کند؛ فقط دیده نمی‌شوند.

از منوی **«مراقبت از خانه»** در خود شیت هم می‌توانی هر وقت خواستی:

| گزینه | کار |
|---|---|
| ساخت / بازسازی برگه‌ها | همان `setup` |
| بازسازی گزارش‌ها | گزارش‌ها را از روی `logs` دوباره می‌سازد |
| مرتب کردن ستون‌ها | عرض ستون‌ها و قالب همهٔ برگه‌ها را دوباره تنظیم می‌کند |
| نمایش ستون‌های فنی | ستون‌های پنهان `logs` را برمی‌گرداند |

---

# نوبت‌ها چطور به هم وصل می‌شوند

هر ثبتی که انجام می‌دهی، بر اساس **تاریخ و ساعت خودش** به آخرین چک‌این آن شخص قبل از خودش وصل می‌شود.
یعنی اگر روز شنبه چک‌این کرده باشی و سه‌شنبه یک کار ثبت کنی، آن کار به همان چک‌این شنبه می‌چسبد —
لازم نیست چک‌این الکی بزنی. چک‌اوت هم به همان نوبتِ باز بسته می‌شود و مدت حضور از روی همان دو حساب می‌شود.

در برگهٔ `logs` ستون **«نوبت (چک‌این)»** دقیقاً می‌گوید هر سطر به کدام چک‌این مربوط است،
و برگهٔ `نوبت‌ها` همه را در قالب یک ردیف برای هر نوبت جمع می‌کند.
اگر کسی بدون هیچ چک‌اینی چیزی ثبت کند، آن ردیف با برچسب **«بدون چک‌این»** جدا نشان داده می‌شود تا آمار قاطی نشود.

# عیب‌یابی

| نشانه | علت و راه‌حل |
|---|---|
| «رمز اشتباه است» | `TOKEN` اسکریپت با رمز داخل اپ یکی نیست |
| ⚠️ آفلاین با اینکه اینترنت وصل است | آدرس به `/exec` ختم نمی‌شود، یا در Deploy گزینهٔ **Anyone** انتخاب نشده |
| تغییری که در اسکریپت دادم اثر ندارد | باید نسخهٔ جدید منتشر شود: **Deploy ← Manage deployments ← ✏️ ← Version: New version ← Deploy** |
| «Execution completed» دیدم ولی شیت خالی ماند | تابع انتخاب‌شده `setup` نبوده، یا شیت refresh نشده — بخش ۲.۳ |
| خطای «این اسکریپت به هیچ Google Sheet وصل نیست» | پروژهٔ اسکریپت جدا ساخته شده؛ باید از داخل خود شیت با **Extensions ← Apps Script** بازش کنی |
| خطای «این تابع برای درخواست‌های اپ است» | `doGet`/`doPost` را دستی Run کرده‌ای؛ در منوی کشویی `setup` را انتخاب کن |
| کادر قرمز «اجازهٔ اتصال ندارد» | اپ را از لینک claude.ai باز کرده‌ای؛ از لینک GitHub Pages استفاده کن |
| ۴۰۴ روی لینک GitHub Pages | build هنوز تمام نشده یا شاخه/پوشهٔ اشتباه انتخاب شده (بخش ۱.۲) |
| اطلاعات یک گوشی نمی‌آید | در همان گوشی: تنظیمات ← **🔄 همگام‌سازی الان** و پیام خطا را بخوان |
| ثبت‌ها دوباره برمی‌گردند بعد از حذف | حذف در گوشی دیگری هنوز همگام نشده؛ در هر دو گوشی یک بار همگام‌سازی کن |
| قبلاً شیت را ساخته‌ای و ستون «نوبت (چک‌این)» را نداری | نسخهٔ جدید `Code.gs` را بچسبان و **New version** منتشر کن؛ ستون‌های تازه از ثبت بعدی پر می‌شوند |

## نکتهٔ امنیتی

هر کسی که لینک `/exec` و رمز را داشته باشد می‌تواند بخواند و بنویسد. اگر لینک لو رفت:
در Apps Script رمزِ `TOKEN` را عوض کن، **New version** منتشر کن، و لینک اشتراکی جدید را برای بقیه بفرست.
